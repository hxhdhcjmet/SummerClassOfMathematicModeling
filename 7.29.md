# 实例讲解视频
## 微分方程
### 微分方程模型的计算简化
#### 热传导方程
- $E(t_2)-E(t_1)$=边界上流入的热量+内部热源产生的热量
- 利用对称性进行计算(简化):区域内没点处的微分方程模型不变；边界条件要给定
   - 矩形烤盘对称算1/4
   - 圆形烤盘温度中心对称，取1/4不如取一条半径，变为一维
 - 一维微分方程可以转换为二维偏微分方程(当只有二维求解工具，没有一维求解工具时)



#### NS方程
- 利用周期性进行计算
- 周期边界条件在离散时出口和入口离散网格相同(保证周期性)

建立微分方程过程中，边界条件的设定也是非常重要的环节
利用已知信息(对称性，周期性)，可以减少计算量


## 数学建模中的优化模型与求解方法
- 线性规划模型与求解方法
- 整数规划模型与求解方法
- 非线性规划模型与求解方法
- 多目标模型与求解方法
- 网络优化模型与求解方法
- Lingo软件与Matlab软件求解



### 线性规划模型与求解方法
- 标准模型
\[\underset{x}{min}f^Tx \\
    s.t. \left\{\begin{matrix}
A \cdot x \le b& \\
  Aeq \cdot x =beq  & \\
  lb \le x \le ub&  &
\end{matrix}\right. \]
$f$为价值向量，$b$为资源向量，$A$，$Aeq$分别为不等式约束和等式约束，$lb$,$ub$为决策变量的下届向量和上界向量
- 若线性规划问题存在可行域，则可行域$D$是凸集(任意凸集内两点的线性组合一定也是原问题的可行解，仍在凸集内)
- 若线性规划问题的可行域$D$有界，则问题的最优解一定在可行域$D$的顶点上达到
- Lingo(交互式线性和通用优化求解器)

### 整数规划模型与求解
- 标准形式
- \[min(max)\sum_{j=1}^{n}c_jx_j \\
    s.t. \left\{\begin{matrix}
 \sum_{j=1}^{n}a_{ij}x_j \le(=,\ge) b_i(i=1,2,\cdots m),\\
  x_j \ge 0,x_j为整数(j=1,2,\cdots,n).\\
\end{matrix}\right. \]

#### 0-1整数规划模型
- 标准形式
- \[min(max)\sum_{j=1}^{n}c_jx_j \\
    s.t. \left\{\begin{matrix}
 \sum_{j=1}^{n}a_{ij}x_j \le(=,\ge) b_i(i=1,2,\cdots m),\\
  x_j=0或1.\\
\end{matrix}\right. \]
- 指派问题：典型0-1问题



### 非线性规划模型与求解方法
- 无约束非线性规划
 \[ \left\{\begin{matrix} minf(x)\\x \in R^n \end{matrix}\right. \]
- 无约束优化问题有局部最优解的充分条件：设$f(x)$有连续的二阶偏导数，点$x^*$满足$\nabla f(x^*)=0$且$\nabla ^2f(x^*)$是正定阵，则$x^*$是无约束优化问题的局部最优解

### 二次规划
- 如果规划模型的目标函数是决策向量$X=(x_1,x_2,\cdots,x_n)^T$的二次函数，约束条件都是线性的，那么这个模型称为二次规划(OP)模型。Ervin规划模型一般形式为：
$$max(min)\sum_{i=1}^{n}\sum_{j=1}^{n}c_{ij}x_ix_j+\sum_{i=1}^{n}d_ix_i\\
s.t.
\left \{ 
\begin{array}{l}
\sum_{j=1}^{n}a_{ij}x_j \le(\ge,=)b_j,i=1,2,\cdots,m \\
x_j\ge 0,i=1,2,\cdots,n
\end{array}
\right.
$$
式中$a_{ij}=a_{ji}$,$i,j=1,2,\cdots,n$。
- 非线性规划求解时使用matlab求解，无约束条件可以使用函数fminunc、fminsearch,有约束条件使用fmincon。也可使用基于问题求解，此时注意非线性规划必须初值。



### 目标优化模型与求解方法
  