# 概率方法与随机算法
## 概率算法
- 证明带有某种性质$P$的特定组合结构存在
- 直接构造：给出具体的构造或确定性的算法
- 概率算法：以概念工具给出一种存在性的证明
- 随机变量的期望
   - $E(X)=\sum_{i}i.Pr(X=i)$
   - $E(XY)=E(X)E(Y)+Cov(X,Y)$
   - $E(X+Y)=E(X)+E(Y)$
   - 期望的线性性质：
\[  E(\sum c_iX_i)=\sum c_iE(X_i)\]
   - 令$X_i$为事件“$\pi(i)=i$”的指示变量
   - $X=X_1+X_2+\cdots+X_n$
   - $E(X)=E(X_1)+E(X_2)+\cdots+E(X_n)=\frac{1}{n} \times n=1$
   - Coupon Collector
## 随机算法
 - 近似算法(Approximation algorithms)
 - Las Vegas:永远保持正确，但运行时间堆积不确定
 - Monte Carlo:结果可能有误，运行时间一般是固定的
    - 单边错误
    - 双边错误
- 寻找中位数

## 图的独立集
- 图$G=(V，E)$
- 设$U$是是顶点集$V$的一个子集,若$U$中任意两点之间没有连边，则$U$称为图的一个独立集(independent set)
- 图的最大独立集的大小称为图的独立数，记为$\alpha(G)$
- 图的独立数的下界:设图的各顶点度数为$d_i,1\le i \le n$,则有
  \[  \alpha(G)\ge \sum_{i=1}^{n}\frac{1}{1+d_i} \]
- peterson图的独立数为4
- 令$V(G)=\{1,2,\cdots,n \}$,以概率$\frac{1}{n!}$随机取一个置换$\pi$
- 令事件$A_i$为"对于点$i$的$d_i$个邻居中的每个点$j$，有\pi^{-1}(j) >\pi^{-1}(i)"
- $Pr(A_i)=(n,d_i+1)\frac{d_i!(n-d_i-1)!}{n!}=\frac{1}{1+d_i}$
- 令$S$为点集$\{$ $i:A_i$发生 $\}$,$S$一定是一个独立集  
- 期望的线性性质$E||S||=\sum E(A_i)=\sum_{i=1}^{n}\frac{1}{1+d_i}$
- 随机算法方便快捷，但只能以一定概率找到符合要求的独立集