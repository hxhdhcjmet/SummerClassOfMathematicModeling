# 插值方法
## 一维插值
- 由离散的、不连续的数据点恢复出实际问题中应该是的连续光滑曲线。
- 函数表达式未知，只有部分观测点，希望给出方法估算出整个观测区域内任意位置上的点。
- 定义：
   - 已知函数$y=f(x)$的$n+1$个互异观测点

  \[  x_0,x_1,\cdots,x_n \]
  上的函数值$y_{i}=f(x_i)$,要求构造一条曲线$\varphi(x) $使得
  \[\varphi(x_i)=y_i,i=0,1,\cdots,n\]
  即求一条近似曲线$\varphi(x)$使得其通过所有数据点$(x_i,y_i),i=0,1,\cdots,n$.。
  - 非观测点用$\varphi(\hat{x})$的值作为$f(\hat{x})$的近似估计值，即$\varphi(\hat{x})\approx f(\hat{x})$。此类问题称为插值问题，构造近似函数的方法称为插值方法。
- 使用多项式插值，构造$n$次多项式：
\[  P_n(x)=a_0+a_1x+\cdots+a_n x^n \]
- 带入已知点，构造出来的方程系数矩阵的行列式是范德蒙德行列式，方程有唯一解
- 但当$n$较大，数据较多时，插值函数的次数会逐渐增大，线性方程组会变成病态方程组，求解结果变得不可靠
## 拉格朗日(Lagrange)插值法
\[  l_i(x)=\frac{(x-x_0)\cdots(x-x_{i-1})(x-x_{i+1})\cdots(x-x_{x_n})}{(x_i-x_0)\cdots(x-x_{i-1})(x-x_{i+1})\cdots(x-x_n)}=\Pi_{j=0,j\ne i}^{n}\frac{x-x_j}{x_i-x_j},i=0,1,\cdots,n \]
$l_i(x)是$n$次多项式,满足
\[ l_i(x_j)=0(j\ne i),1(j=i)\]
则
\[  L_n(x)=\sum_{i=0}^{n}y_il_i(x)=\sum_{i=0}^{n}y_i(\prod_{j=0,j\ne i}^{n}\frac{x-x_j}{x_i-x_j}) \]

上式称为$n$次拉格朗日插值多项式

## 分段插值
- 分段线性插值(解决多项式插值次数过高出现的龙格振荡现象)
- 三次样条插值
# matlab代码实现
- vq=interp1(x0,y0,xq,method,extrapolation) %x0为已知插值节点，y0为对应x0处的函数值,xq是欲求函数值的节点坐标，vq是求得的节点xq处的函数值,method指定插值方法，默认为线性插值(linear),三次样条插值为'spline'
- interp2为二维插值
- 三次样条插值可以使用csape函数,返回pp结构
   - pp=csape(x0,y0) %使用默认边界条件
   - pp=csape(x0,y0,conds,valconds) %conds指定插值边界条件,如果边界条件为一阶导数(complete)，一阶导数的值在valconds参数中给出
   - y=fnval(pp,x) %计算三次样条插值函数pp在x处的取值

# 拟合方法
- 与插值很类似，但此时构造的函数$f(x)$不用过所有观测点，但要求该函数在某种准则下与所有数据点最为接近
\[  \delta_i=f(x_i)-y_i,i=1,2,\cdots,n \]
称$\delta_i$为拟合函数$f(x)$在$x_i$点处的偏差或残差,使$f(x)$整体上尽可能与给定数据最为接近，可以采用“偏平方和最小”作为判定准则，即使
\[  J=\sum_{i=1}^{n}(f(x_i)-y_i)^2 \]
达到最小值。该方法称为最小二乘原则，由此原则拟合函数$f(x)$的方法称为最小二乘法
- 求解
  - 给定线性无关函数系$\{\varphi_k(x)|k=1,2,\cdots,m\}$
  \[ f(x)=\sum_{k=1}^{m}a_k\varphi_k(x)  \]

目标函数$J=J(a_1,a_2,\cdots,a_m)$是关于参数$a_1,a_2,\cdots,a_m$的多元函数,由
  \[  \frac{\partial J}{\partial a_k},k=1,2,\cdots,m,\]

得到关于$a_1,a_2,\cdots,a_k$的线性方程组，称为正规线性方程组。
记为：
\[  R=\left(  \begin{matrix}\varphi_1(x_1)&\varphi_2(x_1)&\cdots&\varphi_m(x_1)\\\varphi_1(x_2)&\varphi_2(x_2)&\cdots&\varphi_m(x_2)\\ \vdots&\vdots& &\vdots \\\varphi_1(x_n)&\varphi_2(x_n)&\cdots&\varphi_m(x_n) \end{matrix} \right),A=\left(  \begin{matrix} a_1\\a_2\\ \vdots \\ a_m \end{matrix} \right),Y=\left(  \begin{matrix}y_1\\y_2\\ \vdots \\y_n \end{matrix} \right)  \]
- 正规方程组表示为：
  \[ R^TRA=R^TY \]
- 当$R$列满秩时$R^TR$可逆，方程组有唯一解，此即为拟合函数系数
  \[ A=(R^TR)^{-1}R^TY \]
也即得到了最小二乘拟合函数$f(x)$
# matlab代码实现：
  - p=polyfit(x,y,n) %拟合n次多项式，返回p是对应多项式系数，顺序为高次幂到低次幂，x,y为对应点的列矩阵
  - y=polyvar(p,x) %计算多项式在x处的函数值

