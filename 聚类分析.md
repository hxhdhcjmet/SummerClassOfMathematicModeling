# 多元分析
## 主成分分析
对于$n$个样本$m$项指标，希望使用较少的变量解释原资料中的大部分变异，即将原本相关性很高的变量转换成彼此相互独立或不相关的变量。通常选出比原始变量个数少的，能解释大部分资料的变异的几个新变量，即所谓的主成分，以此解释资料的综合性指标，本质上主成分分析是一种降维方法。
具体步骤：
- 原数据标准化：
  \[  \tilde{x_{ij}}=(\frac{x_i-\mu_i}{\sigma_i})   \]
- $\mu_i$为平均值,$\sigma_i$为标准差,标准化是为了解决数据量纲和初始相差方差过大的问题，得到表转化数据矩阵$\tilde{A}$。
- 计算相关系数矩阵$R=\tilde{A}^T\tilde{A}/(n-1)$
- 计算相关系数矩阵的特征值和特征向量(单位正交)
- 选取特征值最大的几个作为主成分，注意特征值信息贡献率和累计贡献率都要求较大，符合要求。
- 构建主成分：
\[  z_i=c_{i1}X_1+c_{i2}X_2+\cdots+c_{ip}X_p \]
$z_i$是第$i$个主成分,$c=(c_{i1},c_{i2},\cdots,c_{ip})$为对应特征值的正交单位特征向量
- matlab代码：
  - zscore()%对矩阵进行标准化处理
  - corrcoef()%计算相关系数矩阵
  - [vec,lambda,rate]=pcacov(R)%针对相关系数矩阵R,vec为R的特征向量，lambda为R的特征值,rate为各个主成分的贡献率

## 因子分析
- 因子分析中将可观测数据分解为公共因子、特殊因子、和误差三部分。
- 因子是抽象的、无法直接测得的
- 因子分析关键在于估计因子载荷、方差，并给出因子的合理解释，如果解释困难，需要进行因子旋转。
- 数学模型;
  - 设$p$个变量$X_i,(i=1,2,\cdots,p)$可以表示为：
\[   X_i=\mu_i+\alpha_{i1}F_i+\cdots+\alpha_{im}+F_m+\varepsilon _i,m \le p \]
或：
\[  \left(\begin{matrix} X_1\\X_2 \\ \vdots\\ X_p \end{matrix}\right)=\left(\begin{matrix} \mu_1\\ \mu_2 \\ \vdots\\ \mu_p \end{matrix}\right)+\left(\begin{matrix}\alpha_{11}&\alpha_{12}&\cdots& \alpha_{1m} \\   \alpha_{21}&\alpha_{22}&\cdots&\alpha_{2m} \\ \vdots&\vdots&\ddots&\vdots\\ \alpha_{p1}&\alpha_{p2}&\cdots&\alpha_{pm} \end{matrix}\right) \left(\begin{matrix} F_1\\F_2 \\ \vdots\\ F_p \end{matrix}\right)+\left(\begin{matrix} \varepsilon_1\\ \varepsilon_2 \\ \vdots\\ \varepsilon_p \end{matrix}\right)  \]
或
\[   X-\mu=\Lambda F+\varepsilon\]
$F_1,F_2,\cdots F_p$为公共因子，是不可观测的变量，其系数为载荷因子。$\varepsilon_i$是特殊因子，是不能被前$m$个公共因子包含的部分，且满足
\[  E(F)=0,E(\varepsilon)=0,Cov(F)=I_m,D(\epsilon)=Cov(\epsilon)=diag(\sigma_1^2,\sigma_2^2,\cdots,\sigma_m^2),Cov(F,\epsilon)=0 \]
- 因子载荷矩阵中几个统计意义
  - 因子载荷$\alpha_{ij}$:因子载荷$\alpha_{ij}$是第$i$格变量与第$j$个公共因子的相关系数,反应第$i$个变量与第$j$个公共因子的相关重要性。绝对值越大,相关的密切程度越高。
  - 变量$X_i$的共同度：因子载荷矩阵的第$i$行的元素的平方和，记为$h_i^2=\sum_{j=1}^{m}\alpha_{ij}^2$
由
\[Var(X_i)=\alpha_{i1}^2Var(F_1)+\cdots+\alpha_{im}Var(F_m)+Var(\varepsilon_i)\]
即得
\[  1=\sum_{j=1}^{m}\alpha_{ij}^2+\sigma_i^2=h_i^2+\sigma_i^2 \]
式中特殊因子得方差为$\sigma_i^2(i=1,2,\cdots,p)$称为特殊方差
  - 可见所有公共因子和特殊因子对变量$X_i$的贡献为1.如果$\sum_{j=1}^{m}\alpha_{ij}^2$非常接近1,$\sigma_i^2$非常小，则因子分析的效果好，从原变量到公共因子空间的转换效果好。
  - 因子载荷矩阵各列元素的平方和
\[ S_j=\sum_{i=1}^{p}{\alpha_{ij}^2} \]
称为$F_j(j=1,2,\cdots,m)$对于所有$X_i$的方差贡献和，用于衡量$F_j$的相对重要性。
- 因子分析中的基本问题在于如何估计载荷因子矩阵，即如何求解因子模型
### 求解
#### 主成分分析法
- 设$\lambda_1 \ge \lambda_2 \ge \cdots \ge \lambda_p$为样本相关系数矩阵$R$的特征值，$\eta_1,\eta_2,\cdots,\eta_p$为相应的标准正交化特征向量。设$m<p$，则因子载荷矩阵为:
\[  \Lambda=(\sqrt\lambda_1\eta_1,\sqrt\lambda_2\eta_2,\cdots,\sqrt\lambda_p\eta_p) \],
特殊因子方差用$R-\Lambda^T\Lambda$来估计
\[    \sigma_i^2=1-\sum_{j=1}^{m}\alpha_{ij}^2=1-h_i^2 \]
#### 主因子法
- 主因子法是对主成分法的修正，假定对变量进行了标准化变换，则
\[   R=\Lambda\Lambda^T+D \]
$D=diag\{\sigma_1^2,\sigma_2^2,\cdots,\sigma_m^2\}$
记
\[  R^*=\Lambda\Lambda^T=R-D \]
该式中$R^*$为相约关系矩阵，$R^*$对角线上的元素是$h_i^2$
- 实际问题中特殊因子的方差未知,可通过一组样本来估计，估计方法如下
   - 取$\hat{h_i^2}=1$，这种情况下主因子解与主成分解等价。
   - 取$\hat{h_i^2}=\underset{j\ne i}{max}{|r_{ij}|}$,这意味着取$X_i$与其余$X_j$的简单相关系数的绝对值最大者
记
\[  R^*=R-D=\left(\begin{matrix}\hat h_1^2&r_{12}&\cdots&r_{1p}\\r_{21}&\hat h_2^2&\cdots&r_{2p}\\ \vdots&\vdots & \ddots& \vdots \\ r_{p1}&r_{p2}&\cdots&\hat h_p^2,    \end{matrix} \right) \]
直接求$R^*$得的前$p$个特征值$\lambda_1^* \ge \lambda_2^*\ge \cdots \ge \lambda_p^*$,和对应的正交特征向量$u_1^*,u_2^*,\cdots,u_p^*$,得到载荷因子矩阵
\[  \Lambda=(\sqrt \lambda_1^*u_1^* \sqrt \lambda_2^*u_2^* \cdots \sqrt \lambda_p^*u_p^* ) \]
#### 最大似然估计法
- 直接用matlab中的factoran实现
## 因子旋转
- 每个公共因子含义不清楚，不便于进行实际背景的解释。载荷因子矩阵不唯一，所以应对因子载荷矩阵进行旋转，使因子载荷矩阵结构简单化，使载荷矩阵每列或每行的元素平方值向0和1量及分化，主要方法有：方差最大法，四次方最大法和灯亮最大法。
- 使用matlab实现代码：
    -[lambda2,t]=rotatefactors(lambda(1:num),'method','varimax') %lambda2为旋转后的载荷因子矩阵，t为变换的正交矩阵。lambda为原全部载荷因子矩阵，num为取多少个主因子，varimax为使用最大方差旋转
## 因子得分
- 因子分析模型给出了原变量表示为公共因子的线性组合，但通产情况下还想反过来把公共因子表示为原变量的线性组合。
- 因子得分函数
  \[  F_j=c_j+\beta_{j1}X_1+\cdots+\beta_{jp}X_p,j=1,2,\cdots,p \]
- 因子得分实现关键在于得分矩阵的求解，由于$p>m$该方程无法求得精确解，只能通过估计
- 常用方法有巴特莱特因子得分法(加权最小二乘法)、回归方法
  - 巴特莱特因子得分法：
     - 将$X_i-\mu_i$看作因变量，因子载荷矩阵看作自变量的观测，由于特殊因子方差相异，用最小二乘法求得分，使
\[  \sum_{i=1}^{p}[(X_i-\mu_i)-(\alpha_{i1}\hat F_1+\alpha_{i2}\hat F_2+\cdots+\alpha_m\hat F_m)]^2/\sigma_i^2 \]
最小的$\hat F_1,\hat F_2\cdots,\hat F_m$是相应的因子得分。
     - 矩阵表达为
  \[ X-\mu=\Lambda F+\varepsilon\]
要使得
\[  (X-\mu-\Lambda F)^TD^{-1}(X-\mu-\Lambda F) \]
达到最小，其中$D=diag\{ \sigma_1^2,\sigma_2^2,\cdots,\sigma_p^2 \}$,上式取得最小值的$F$是相应的一因子得分
     - 计算得
\[    \hat F=(\Lambda^T D^{-1}\Lambda)^{-1}\Lambda^TD^{-1}(X-\mu) \]
  - 回归方法
      - 回归方法将公共因子视为因变量，原始变量视为自变量，通过最小二乘法(OLS)求解得分系数矩阵，使因子得分与原始变量误差最小。该方法不考虑特殊因子的影响(假设$\varepsilon_i=0$),计算简单
      - 得到的因子得分估计为
\[  \hat F=X_0R^{-1}\Lambda \]  
$X_0$为原始数据矩阵，$R$为原始变量的相关系数矩阵，$\Lambda$为因子载荷矩阵
  - matlab求解
     - [\~,\~,\~,factor_score]=factoran(data,n_factors) % data为原始数据矩阵，factor_scores为得分矩阵,fractoran为关键函数,n_factors为因子个数
  

## 判别分析


## 典型相关分析
一般研究量组变量
\[(x_1,x_2,\cdots,x_p),(y_1,y_2,\cdots,y_q) \]
的相关关系时，最原始的方法为分别计算量组变量之间的全部相关系数，一共有$pq$个简单相关系数,该方法繁琐而又无法抓住问题本质。借助主成分分析的思想，找出两组变量的线性组合作为新的变量，讨论新的变量(线性组合)之间的相关关系，可以达到简化问题、降低维度的目的。
- 分别在量组变量中找出第一对线性组合，使得其具有最大相关关系，即
$$
\begin{equation}
\left\{
\begin{aligned}
\nonumber
u_1=&\alpha_{11}x_1+\alpha_{21}x_2+\cdots+\alpha_{p1}x_p,\\
v_1=&\beta_{11}y_1+\beta_{21}y_2+\cdots+\beta_{q1}y_q
\end{aligned}
\right.
\end{equation}
$$
- 然后在每组变量中找出第二对线性组合，使其分别与本组内第一队线性组合不相关，第二对线性组合本身具有次大的相关性，有
$$
\begin{equation}
\left\{
\begin{aligned}
\nonumber
u_2=&\alpha_{21}x_1+\alpha_{22}x_2+\cdots+\alpha_{p2}x_p,\\
v_2=&\beta_{21}y_1+\beta_{22}y_2+\cdots+\beta_{q2}y_q
\end{aligned}
\right.
\end{equation}
$$
满足$u_1$与$u_2$，$v_1$与$v_2$不相关，但$u_2$与$v_2$相关。如此继续下去，直至到第$r$次两组变量的相关性被提取完为止，可以得到$r$组变量，$r\le min(p,q)$。
- 使用复相关系数(全相关系数)研究两组随机变量之间的相关关系。
- 复相关系数描述两组随机变量$X=(x_1,x_2,\cdots,x_p)^T$与$Y=(y_1,y_2,\cdots,y_p)^T$之间的相关程度，其思想为先将每一组随机变量作线性组合，成为两个随机变量
\[  u=\rho^TX=\sum_{i=1}^{p}\rho_ix_i,v=\gamma^TY=\sum_{i=1}^{q}\gamma_iy_i \]
z再研究$u$与$v$的相关系数。由于$u$，$v$与投影向量$\rho$,$\gamma$有关，故$r_{uv}=r_{uv}(\rho,\gamma)$。取在$\rho^T\Sigma_{XX}\rho=1$和$\gamma\Sigma_{YY}\gamma=1$的条件下使$r_{uv}$达到最大的$\rho,\gamma$作为投影向量,得到的相关系数即为复相关系数
\[    r_{uv}=\max\ r_{uv}(\rho,\gamma) \]
- 两组协方差矩阵分块的得
\[  Cov
\begin{pmatrix}
 X\\Y\end{pmatrix}= \begin{pmatrix}
  Var(X)&Cov(X,Y) \\
  Cov(Y,X)&Var(Y)
\end{pmatrix}=\begin{pmatrix}
  \Sigma_{XX}&\Sigma_{XY} \\
  \Sigma_{YX}&\Sigma_{YY}
\end{pmatrix}\]
得
\[   r_{uv}=\frac{Cov(\rho^TX,\gamma^TY)}{\sqrt {D(\rho^TX)}\sqrt{D(\gamma^TY)}}=\frac{\rho^T\Sigma_{XY}\gamma}{\sqrt{\rho^T\Sigma_{XX}\rho}\sqrt{\gamma^T\Sigma_{YY}\gamma}}=\rho^T\Sigma_{XY}\gamma \]
- 问题转化为在$\rho^T\Sigma_{XX}\rho=1$和$\gamma^T\Sigma_{YY}\gamma=1$的条件下球$\rho^T\Sigma_{XY}\gamma$的极大值
- 根据条件极值求法，引入拉格朗日乘数，问题转换为求
\[   S(\rho,\gamma)=\rho^T\Sigma_{XY}\gamma-\frac{\lambda}{2}(\rho^T\Sigma_{XX}\rho-1)-\frac{\omega}{2}(\gamma^T\Sigma_{YY}\gamma-1) \]
的极大值，$\lambda$,$\omega$是拉格朗日乘数
- 结合极值必要条件有
\[  
\left\{\begin{matrix}
 \frac{\partial S}{\partial \rho}=\Sigma_{XX}\gamma-\lambda\Sigma_{XX}\rho=0\\
 \\
 \frac{\partial S}{\partial \gamma}=\Sigma_{YX}\rho-\omega\Sigma_{YY}\gamma=0
\end{matrix}\right. \]
- 结合该必要条件可得
  \[   M_1\rho=\lambda^2\rho,M_2\gamma=\lambda^2\gamma \]
其中$M_1=\Sigma_{XX}^{-1}\Sigma_{XY}\Sigma_{YY}^{-1}\Sigma_{YX},M_2=\Sigma_{YY}^{-1}\Sigma_{YX}\Sigma_{XX}^{-1}\Sigma_{XY}$
- 以上说明$\lambda^2$既是$M_1$又是$M_2$的特征值，$\rho$,$\gamma$就是其相应于$M_1$和$M_2$的特征向量。$M_1$,$M_2$的特征值非负且都在[0,1]上,非零特征值的个数等于$\min(p,q)$,不妨设为$q$。
- 设$M_1\rho=\lambda^2\rho$的特征值排序为$\lambda_1^2\ge \lambda_2^2\ge\cdots\ge \lambda_q^2$,其余$p-q$个特征值为0，称$\lambda_1^2,\lambda_2^2,\cdots, \lambda_q^2$为典型相关系数。相应的从$M_1\rho=\lambda^2\rho$和$M_2\gamma=\lambda^2\gamma$解出的特征向量$\rho^{(1)},\rho^{(2)},\cdots,\rho^{(q)}$和$\gamma^{(1)},\gamma^{(2)},\cdots,\gamma^{(q)}$,得到$q$对线性组合
\[  u_i=\rho^{(i)T}X,v_i=\gamma^{(i)T}Y,i=1,2,\cdots,q\]
称每一对变量为典型变量。求典型相关系数和典型变量即为求$M_1,M_2$得特征值和特征向量。
- 还可证明，当$i \ne j$时，有
\[
\left\{\begin{matrix}
 Cov(u_i,u_j)=Cov(\rho^{(i)T}X,\rho^{(j)T}X)=\rho^{(i)T}\Sigma_{XX}\rho^{(j)}=0\\
Cov(v_i,v_j)=Cov(\gamma^{(i)T}Y,\gamma^{(j)T}Y)=\gamma^{(i)T}\Sigma_{YY}\gamma^{(j)}=0
\end{matrix}\right.\]
表明一切典型变量都是不相关的,并且方差为1，即
\[
\left\{\begin{matrix}
Cov(u_i,u_j)=\delta_{ij}\\
Cov(v_i,v_j)=\delta_{ij}
\end{matrix}\right.\]
其中
\[
\delta_{ij=}\left\{\begin{matrix}
 1,i=j\\
0,i \ne j
\end{matrix}\right.\]
- $X$与$y$的同一对典型变量$u_i$和$v_i$之间的相关系数为$\lambda_i$,不同对的典型变量$u_i$和$v_j$$(i\ne j)$之间也不相关,也即协方差为0.即
\[  Cov(u_i,v_i)=\left\{\begin{matrix}
\lambda_i,i=j\\
0,i\ne j
\end{matrix}\right. \]
### 原始变量与典型变量之间的相关性
- 原始变量相关系数矩阵
\[  R=\begin{pmatrix}
  R_{11}&R_{12} \\
  R_{21}&R_{22}
\end{pmatrix} \]
- $X$典型变量相关系数矩阵
\[ \Lambda=(\rho^{(1)},\rho^{(2)},\cdots,\rho^{(s)})_{p \times s}=
\begin{pmatrix}
\alpha_{11}&\alpha_{12}  & \cdots  & \alpha_{1s} \\
\alpha_{21}&\alpha_{22} & \cdots  &\alpha_{2s} \\
\vdots& \vdots & \ddots  & \vdots \\
\alpha_{p1}&\alpha_{p2}  & \cdots  &\alpha_{ps}
\end{pmatrix}
\]
- $Y$典型变量系数矩阵
\[ \Gamma=(\gamma^{(1)},\gamma^{(2)},\cdots,\gamma^{(s)})_{q\times s}=
\begin{pmatrix}
\beta_{11}&\beta_{12}  & \cdots  & \beta_{1s} \\
\beta_{21}&\beta_{22} & \cdots  &\beta_{2s} \\
\vdots& \vdots & \ddots  & \vdots \\
\beta_{q1}&\beta_{q2}  & \cdots  &\beta_{qs}
\end{pmatrix}\]
则有
\[  Cov(x_i,u_j)=Cov(x_i,\sum_{k=1}^{p}\alpha_{kj}x_k)=\sum_{k=1}^{p}\alpha_{kj}Cov(x_i,x_k),j=1,2,\cdots,s \]
$x_i$与$u_j$的相关系数
\[  r(x_i,u_j)=\sum_{k=1}^{p}\alpha_{kj}Cov(x_i,x_k)/\sqrt{D(x_i)},j=1,2,\cdots,s \]
同理有
\[  \begin{align*}
   r(x_i,v_j)&=\sum_{k=1}^{p}\alpha_{kj}Cov(x_i,y_k)/\sqrt{D(x_i)},j=1,2,\cdots,s \\
    r(y_i,u_j)&= \sum_{k=1}^{q}\beta_{kj}Cov(y_i,x_k)/\sqrt{D(y_i)},j=1,2,\cdots,s \\
    r(y_i,v_j)&= \sum_{k=1}^{q}\beta_{kj}Cov(y_i,y_k)/\sqrt{D(y_i)},j=1,2,\cdots,s
\end{align*} \]
- 各组原始变量被典型变量所解释的方差
  -  $X$组原始变量被$u_i$解释的方差比例为
\[m_{ui}=\sum_{k=1}^{p}r^2(u_i,x_k)/p. \]
  - $X$组原始变量被$v_i$解释的方差比例为
\[m_{v_i}=\sum_{k=1}^{p}r^2(v_i,x_k)/p. \]
  - $ Y$组原始变量被$u_i$解释的方差比例为
\[n_{u_i}=\sum_{k=1}^{q}r^2(u_i,y_k)/q. \]
  - $Y$组原始变量被$v_i$解释的方差比例为
\[n_{v_i}=\sum_{k=1}^{q}r^2(v_i,y_k)/q.\]
### 典型相关分析法的基本思路
- 假设有两组随机变量$X=(x_1,x_2,\cdots,x_p)^T,Y=(y_1,y_2,\cdots,y_q)^T$,$C$为$p+q$组总体的$n$次标准化数据阵，有
\[\begin{pmatrix}
 a_{11} &\cdots  &a_{1p}  &b_{11}  &\cdots  &b_{1q} \\
  a_{21}&\cdots  &a_{2p}  &b_{21}  &\cdots  &b_{2q} \\
  \vdots&\ddots  &\vdots  &\vdots  &\ddots  &\vdots \\
  a_{n1}&\cdots  &a_{np}  &b_{n1}  &\cdots  &b_{nq}
\end{pmatrix}\]
- 第一步，计算相关系数矩阵$R$,并将$R$剖分为
\[R=\begin{pmatrix}
  R_{11}&R_{12} \\
  R_{21}&R_{22}
\end{pmatrix}\]
其中$R_{11}$和$R_{22}$分别为第一组变量和第二组变量的相关系数阵，$R_{12}$=$R^T_{21}$为第一组与第二组的相关系数阵。
- 第二步，求典型相关系数及典型变量。首先求$M_1=R^{-1}_{11}R_{12}R^{-1}_{22}R_{21}$的特征值$\lambda^2_{i}$,特征向量$\rho^{(i)}$;$M_2=R^{-1}_{22}R_{21}R^{-1}_{11}R_{12}$的特征值$\lambda^2_{j}$,特征向量$\gamma^{(j)}$则典型变量为
\[ u_1=\rho^{(1)T}X,v_1=\gamma^{(1)T}Y,\cdots,u_s=\rho^{(s)T}X,v_s=\gamma^{(s)T}Y,s=\min(p,q) \]
记$U=(u_1,u_2,\cdots,u_s)^T$,$V=(v_1,v_2,\cdots,v_s)^T$
- 第三步，典型相关系数$\lambda_i$的显著性检验
- 第四步，典型结构与典型冗余分析
### matlab代码
- [R,A,B,U,V,stas]=canoncorr(x,y)
  - R:典型相关系数
  - A:x组变量的典型系数(用于计算x的典型变量U=X*A)
  - B:y组变量的典型系数(用于计算y的典型变量V=Y*B)
  - U,V:分别为x和y的典型变量
  - stas:包含显著性检验结果
  - 如果有需要，需提前对x,y作标准化处理