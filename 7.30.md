# 机器学习反向传播算法
- 逻辑与、或、非用一层神经网络即可表示
- 凸区域用两层神经网络即可表示
- 非凸区域用三层神经网络即可表示(非凸区域可以视作几个凸区域的并，凸区域两层神经网络表示，并一层神经网络表示)
- 区域识别
- 反向传播算法(Back Propagation)学习算法
    - 给定$w_{ij},\theta_i$初始值，选定$E$的终止值，步长控制量$\eta$。
    - 对每个训练样本$\mu_p$，正向计算$x^t_{pi}$,计算误差函数值$E$。
    - 若$E\le \varepsilon$,则算法阶数，否则，