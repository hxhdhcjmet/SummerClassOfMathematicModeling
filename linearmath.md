# 线性代数拓展
## 矩阵可对角化
$A$是对称矩阵，存在正交矩阵$P$和对角矩阵$D$满足：
\[P^TAP=D\]
MATLAB求解命令:
\[[P,D]=eig(A);\]
只要$A$可对角化,不是对称矩阵仍可用上述命令求解。

## 矩阵求逆 
可逆矩阵A,逆矩阵为inv(A)
方程AX=b,使用左除，X=A\比X=inv(A)*b更快
一般矩阵A使用pinv函数计算PM广义逆:
\[G=pinv(A)\]
A的广义逆矩阵与A的转置有相同的尺寸
A的PM广义逆矩阵G用来计算方程$AX=b$的最小模最小二乘解,$x_{0}$=$Gb$